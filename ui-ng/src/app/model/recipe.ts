export class Recipe {
    title: String;
    instructions: String;

    constructor(title: String, instructions: String) {
        this.title = title;
        this.instructions = instructions;
    }
}
